<UserControl x:Class="Amaranth.View.ShowProductPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Amaranth.ViewModel"
             xmlns:s="clr-namespace:Amaranth.Service"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Tag="{Binding DataContext.Product.Title, RelativeSource={RelativeSource Mode=Self}, Converter={StaticResource HeaderProcessingConverter}}">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <s:ListToStringConverter x:Key="ListToStringConverter"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.IsSharedSizeScope="True">
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" SharedSizeGroup="Desc"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="15"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Label Content="Наименование: " Grid.Row="1"/>
            <TextBlock Text="{Binding Product.Title}" Grid.Row="1" Grid.Column="1"/>

            <Label Content="Категория: " Grid.Row="2"/>
            <TextBlock Text="{Binding Product.Category.Title}" Grid.Column="1" Grid.Row="2"/>

            <Label Content="Цена: " Grid.Row="3"/>
            <TextBlock Text="{Binding Product.Price, StringFormat=0.00₽}" Grid.Column="1" Grid.Row="3"/>

            <Label Content="Рецепт: " Grid.Row="4"/>
            <TextBlock Grid.Column="1" Grid.Row="4" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Text" Value= "Нет"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Product.Prescription}" Value="True">
                                <Setter Property="Text" Value= "Да"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Label Content="Теги: " Grid.Row="5"/>
            <TextBlock Text="{Binding Product, Converter={StaticResource ListToStringConverter}}" Grid.Column="1" Grid.Row="5"/>

            <ItemsControl ItemsSource="{Binding Product.Description}" Grid.ColumnSpan="2" Grid.Row="6">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto" SharedSizeGroup="Desc"/>
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <Label Content="{Binding Title}"/>
                            <TextBlock Text="{Binding Value}" Grid.Column="1" VerticalAlignment="Center" TextWrapping="Wrap"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
